<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Black.Theme.GFIT"
        tools:targetApi="35">
        <activity
            android:name=".views.pages.activities.WelcomeActivity1"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.pages.activities.WelcomeActivity2" />
        <activity android:name=".views.pages.activities.WelcomeActivity3" />
        <activity android:name=".views.pages.activities.WelcomeActivity4" />
        <activity android:name=".views.pages.activities.LoginActivity" />
        <activity android:name=".views.pages.activities.SignupActivity" />
        <activity android:name=".views.pages.activities.MainActivity" />
        <activity android:name=".views.pages.activities.WelcomeActivity5"/>
        <activity android:name=".views.pages.activities.FormActivity1" />
        <activity android:name=".views.pages.activities.FormActivity2" />
        <activity android:name=".views.pages.activities.FormActivity3" />
        <activity android:name=".views.pages.activities.FormActivity4" />
        <activity android:name=".views.pages.activities.AfterFormActivity" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>


</manifest>